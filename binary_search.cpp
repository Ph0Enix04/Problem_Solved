#include<iostream>

int main()
{
    // calculate the middle point of the current subarray
    // if the target is at the middle, repeat, changing the end point ot be just to the left of the middle.
    // you can't apply it with unsorted array
    
    return 0;
}